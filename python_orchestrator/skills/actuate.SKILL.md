# Skill: actuate_relay
Description: Activates a physical relay or GPIO pin to control external hardware (e.g. turning on a fan if temperature is high).
Tool Format: {"tool": "actuate_relay", "args": ["pin_number", "state (1 or 0)"]}
